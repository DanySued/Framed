{
  "hooks": {
    "PostToolUse": [
      {
        "matcher": "Write|Edit",
        "hooks": [
          {
            "type": "command",
            "command": "git add -A && git diff --cached --quiet || (git commit -m 'auto: sync' && git push origin master) 2>/dev/null || true",
            "shell": "bash",
            "async": true,
            "statusMessage": "Pushing to main..."
          }
        ]
      }
    ]
  }
}
